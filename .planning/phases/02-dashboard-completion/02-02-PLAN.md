---
phase: 02-dashboard-completion
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - shared/schema.ts
  - server/storage.ts
  - bot/db.py
  - client/src/components/ApprovalQueue.tsx
autonomous: true
must_haves:
  truths:
    - "Each approval shows detailed analysis breakdown"
    - "Analysis includes: model probability, market probability, edge calculation"
    - "Analysis shows source agent and confidence factors"
    - "Analysis data is stored in database when signal is created"
    - "UI displays analysis in expandable or tooltip format"
  artifacts:
    - path: "shared/schema.ts"
      provides: "analysis field in pending_approvals table"
      contains: "analysis: text('analysis')"
    - path: "bot/db.py"
      provides: "add_pending_approval stores analysis JSON"
      exports: ["add_pending_approval"]
    - path: "server/storage.ts"
      provides: "getPendingApprovals returns analysis field"
      exports: ["PendingApproval"]
    - path: "client/src/components/ApprovalQueue.tsx"
      provides: "Analysis breakdown display UI"
      exports: ["ApprovalQueue"]
  key_links:
    - from: "bot/add_pending_approval"
      to: "pending_approvals table"
      via: "INSERT with analysis JSON"
      pattern: "analysis.*json"
    - from: "server/getPendingApprovals"
      to: "ApprovalQueue component"
      via: "API response"
      pattern: "approval\.analysis"
    - from: "ApprovalQueue UI"
      to: "user view"
      via: "rendered analysis section"
      pattern: "Analysis|Details|Breakdown"
---

<objective>
Add detailed analysis breakdown display for each signal in the approval queue, showing model vs market probabilities, edge calculation, and contributing factors.

**Purpose:** Help users understand WHY a signal was generated before approving, increasing confidence in decisions.

**Output:** Each pending approval shows expandable analysis with source reasoning.
</objective>

<execution_context>
@C:/Users/kittu/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/kittu/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@shared/schema.ts
@bot/db.py
@server/storage.ts
@client/src/components/ApprovalQueue.tsx
@bot/signal_engine.py
@bot/agents/base_agent.py
</context>

<tasks>
<task type="auto">
<name>Task 1: Add analysis field to database schema</name>
<files>shared/schema.ts, bot/db.py</files>
<action>
Add an analysis field to store signal breakdown details.

In shared/schema.ts:
1. Add `analysis: text("analysis").default("{}"),` to pendingApprovals table
2. The field stores JSON string with analysis details

In bot/db.py:
1. Update add_pending_approval() to accept and store analysis parameter
2. Update the INSERT statement to include analysis field
3. Default to empty JSON object if not provided
4. Update _prune_approvals if needed to handle new field

After schema changes, run `npm run db:push` to migrate the database.

The analysis JSON structure should include:
- model_prob: number (model's probability estimate)
- market_prob: number (current market price)
- edge: number (model - market)
- factors: string[] (what contributed to the signal)
- confidence: number (0-100 confidence score)
- reasoning: string (human-readable explanation)
</action>
<verify>npm run db:push completes successfully, schema updated</verify>
<done>shared/schema.ts has analysis field, bot/db.py stores analysis in add_pending_approval, database migrated</done>
</task>

<task type="auto">
<name>Task 2: Update storage layer to handle analysis</name>
<files>server/storage.ts</files>
<action>
Update the storage layer to read and return the analysis field.

1. Update PendingApproval interface to include:
   - analysis?: string (optional for backward compatibility)

2. Update getPendingApprovals() to:
   - Select analysis field from database
   - Parse JSON and return as object (or empty object if null/invalid)
   - Return analysis as parsed object, not raw string

3. Update storage type definitions to reflect the new structure

4. Ensure TypeScript strict mode passes (no any types)

Follow existing patterns in storage.ts for field handling.
</action>
<verify>npm run check passes, no TypeScript errors</verify>
<done>PendingApproval interface includes analysis field, getPendingApprovals parses and returns analysis object</done>
</task>

<task type="auto">
<name>Task 3: Display analysis breakdown in ApprovalQueue UI</name>
<files>client/src/components/ApprovalQueue.tsx</files>
<action>
Enhance the ApprovalQueue component to display analysis details for each approval.

Changes:
1. Import UI components:
   - import { Collapsible, CollapsibleContent, CollapsibleTrigger } from "@/components/ui/collapsible"
   - import { ChevronDown, ChevronUp, Info } from "lucide-react"

2. Create AnalysisBreakdown sub-component that shows:
   - Model Probability: {model_prob}%
   - Market Probability: {market_prob}%
   - Edge: {edge}%
   - Confidence Score: {confidence}/100
   - Contributing Factors: list of factors
   - Reasoning: explanation text

3. For each approval card:
   - Add a "View Analysis" button/chevron
   - When clicked, expand to show AnalysisBreakdown
   - Use Collapsible component for smooth expand/collapse
   - Show analysis only if present (backward compatible)

4. Style the analysis section:
   - Use muted text colors for labels
   - Use monospaced/tabular numbers for values
   - Show factors as badges or bullet list
   - Keep compact but readable

5. Update TypeScript types to include analysis field from shared/schema
</action>
<verify>Dashboard shows "View Analysis" button that expands to show breakdown details</verify>
<done>ApprovalQueue displays analysis breakdown with model/market probs, edge, confidence, factors, and reasoning</done>
</task>
</tasks>

<verification>
1. Schema includes analysis field
2. Python bot can store analysis when creating approvals
3. API returns analysis data
4. Dashboard shows "View Analysis" button for each approval
5. Expanded view shows model prob, market prob, edge, confidence, factors, reasoning
</verification>

<success_criteria>
- pending_approvals table has analysis column
- Bot stores analysis JSON when creating signals
- Dashboard displays analysis breakdown per approval
- Analysis includes model/market probabilities, edge, confidence, factors
- UI is clean and information is readable
</success_criteria>

<output>
After completion, create `.planning/phases/02-dashboard-completion/02-02-SUMMARY.md`
</output>
