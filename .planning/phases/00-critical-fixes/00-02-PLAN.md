---
phase: 00-critical-fixes
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - bot/approval_gate.py
  - server/storage.ts
autonomous: true

must_haves:
  truths:
    - "Python writes pending_approvals.json when approval is requested"
    - "TypeScript reads pending_approvals.json and serves to dashboard"
    - "Dashboard ApprovalQueue shows live pending approvals"
  artifacts:
    - path: "pending_approvals.json"
      provides: "Shared state between Python and TypeScript"
      min_lines: 1
  key_links:
    - from: "bot/approval_gate.py"
      to: "pending_approvals.json"
      via: "json.dump"
      pattern: "APPROVALS_FILE"
    - from: "server/storage.ts"
      to: "pending_approvals.json"
      via: "fs.readFileSync"
      pattern: "pending_approvals"
---

<objective>
Wire pending_approvals.json so Python writes it and TypeScript reads it. Dashboard approval queue will then show live pending approvals.
</objective>

<context>
@.planning/ROADMAP.md
@C:\Users\kittu\Downloads\GIT\Pollyedge\pollyedge\bot\approval_gate.py
@C:\Users\kittu\Downloads\GIT\Pollyedge\pollyedge\server\storage.ts
</context>

<tasks>

<task type="auto">
  <name>Write pending_approvals.json from Python approval_gate</name>
  <files>bot/approval_gate.py</files>
  <action>
    Modify `bot/approval_gate.py` to write pending_approvals.json:

    1. Add `import json, os, threading, datetime` if not present
    2. Define module-level constants:
       ```python
       _approvals_lock = threading.Lock()
       APPROVALS_FILE = "pending_approvals.json"
       ```
    3. Add `_write_pending()` method that creates an entry with:
       - id, label, side, size, edge, source, score, market_prob, model_prob, timestamp, status="pending"
       - Keeps last 20 entries
    4. Add `_resolve_pending()` method that updates status to "approved"/"rejected"/"expired"
    5. Call `_write_pending()` BEFORE the poll loop starts
    6. Call `_resolve_pending()` AFTER each decision

    Make it thread-safe with `_approvals_lock`.
  </action>
  <verify>python -c "
import json, os
os.system('python -c \"from bot.approval_gate import _write_pending, _resolve_pending; print(\\\"functions exist\\\")\"')
"</verify>
  <done>approval_gate writes pending_approvals.json when signals need approval</done>
</task>

</tasks>

<verification>
1. Start the bot with a test signal
2. Check `pending_approvals.json` exists and has entries
3. Verify entries have all required fields (id, label, side, size, edge, status="pending")
4. After approval/rejection, status changes to "approved"/"rejected"
</verification>

<success_criteria>
- pending_approvals.json is created when first approval is requested
- Dashboard shows non-empty ApprovalQueue
- Status updates correctly after Telegram approval/rejection
</success_criteria>

<output>
After completion, create `.planning/phases/00-critical-fixes/00-02-SUMMARY.md`
</output>
