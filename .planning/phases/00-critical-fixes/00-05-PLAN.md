---
phase: 00-critical-fixes
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - api/server.py
autonomous: true

must_haves:
  truths:
    - "FastAPI server uses correct STARTING_BALANCE default ($10)"
    - "Bot and API have consistent balance defaults"
  artifacts:
    - path: "api/server.py"
      provides: "FastAPI server with correct balance defaults"
---

<objective>
Fix STARTING_BALANCE default in api/server.py from 500 to 10. Also verify all Python files have load_dotenv() called before os.getenv().
</objective>

<context>
@.planning/ROADMAP.md
@C:\Users\kittu\Downloads\GIT\Pollyedge\pollyedge\api\server.py
@C:\Users\kittu\Downloads\GIT\Pollyedge\pollyedge\bot\approval_gate.py
</context>

<tasks>

<task type="auto">
  <name>Fix STARTING_BALANCE default in api/server.py</name>
  <files>api/server.py</files>
  <action>
    In `api/server.py`, change:
    ```python
    starting_balance = float(os.getenv("STARTING_BALANCE", 500))
    ```
    To:
    ```python
    starting_balance = float(os.getenv("STARTING_BALANCE", 10))
    ```
  </action>
  <verify>grep -n "STARTING_BALANCE" api/server.py</verify>
  <done>FastAPI server defaults to $10 balance when env var not set</done>
</task>

<task type="auto">
  <name>Verify load_dotenv() is called before os.getenv() in all files</name>
  <files>
    - bot/approval_gate.py
    - bot/pollyedge_bot.py
    - bot/orchestrator.py
    - bot/position_monitor.py
  </files>
  <action>
    Check all Python files in the bot/ and api/ directories. Ensure `load_dotenv()` is called at the VERY TOP of the file (before any imports, especially before project imports that might use os.getenv).

    Files that need it:
    - bot/approval_gate.py — verify load_dotenv() at top
    - bot/position_monitor.py (if created) — must have load_dotenv() at top

    If any file uses os.getenv() before load_dotenv(), add `from dotenv import load_dotenv; load_dotenv()` at the very top.
  </action>
  <verify>python -c "
import ast, sys
files = ['bot/approval_gate.py', 'bot/position_monitor.py', 'api/server.py']
for f in files:
    try:
        with open(f) as fp:
            content = fp.read()
        tree = ast.parse(content)
        for node in ast.walk(tree):
            if isinstance(node, ast.Call):
                if hasattr(node.func, 'attr') and node.func.attr == 'getenv':
                    print(f'WARN: {f} uses os.getenv() - verify load_dotenv() is first')
                    break
    except: pass
print('Check complete')
"</verify>
  <done>All files load environment variables correctly</done>
</task>

</tasks>

<verification>
1. Start FastAPI server: `uvicorn api.server:app`
2. Check that balance shows $10 when STARTING_BALANCE env var is not set
3. Verify all Python files start without "KeyError" for missing env vars
</verification>

<success_criteria>
- api/server.py STARTING_BALANCE defaults to 10 (not 500)
- All Python files have load_dotenv() called before using os.getenv()
</success_criteria>

<output>
After completion, create `.planning/phases/00-critical-fixes/00-05-SUMMARY.md`
</output>
