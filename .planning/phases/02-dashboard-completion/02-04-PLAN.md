---
phase: 02-dashboard-completion
plan: 04
type: execute
wave: 3
depends_on: []
files_modified:
  - client/src/components/RulesPanel.tsx
  - server/routes.ts
  - server/storage.ts
  - shared/schema.ts
  - bot/db.py
autonomous: true
must_haves:
  truths:
    - "UI shows slider for conviction threshold (0-100)"
    - "Slider value displays current threshold"
    - "Moving slider updates threshold in real-time"
    - "Threshold is persisted to database"
    - "Bot reads threshold from database and uses it"
    - "Dashboard only shows signals above threshold"
  artifacts:
    - path: "client/src/components/RulesPanel.tsx"
      provides: "Conviction threshold slider UI"
      exports: ["RulesPanel"]
    - path: "server/routes.ts"
      provides: "POST /api/bot/threshold endpoint"
      exports: ["registerRoutes"]
    - path: "server/storage.ts"
      provides: "getThreshold and setThreshold methods"
      exports: ["IStorage"]
    - path: "shared/schema.ts"
      provides: "conviction_threshold in bot_state table"
      contains: "key: 'conviction_threshold'"
  key_links:
    - from: "RulesPanel slider"
      to: "POST /api/bot/threshold"
      via: "mutation onChange"
      pattern: "apiRequest.*threshold"
    - from: "threshold endpoint"
      to: "storage.setThreshold"
      via: "function call"
      pattern: "storage\.setThreshold"
    - from: "bot scan loop"
      to: "get_threshold"
      via: "db.get_bot_state"
      pattern: "conviction_threshold"
    - from: "ApprovalQueue"
      to: "filtered approvals"
      via: "score >= threshold"
      pattern: "approval\.score >= threshold"
---

<objective>
Add a conviction threshold slider to the dashboard that filters which signals appear in the approval queue, with the threshold persisted and used by the bot.

**Purpose:** Allow users to control signal quality — only high-confidence opportunities surface for approval.

**Output:** Working threshold slider that filters dashboard and bot signals.
</objective>

<execution_context>
@C:/Users/kittu/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/kittu/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@client/src/components/RulesPanel.tsx
@server/routes.ts
@server/storage.ts
@shared/schema.ts
@bot/db.py
@bot/approval_gate.py
@client/src/components/ApprovalQueue.tsx
</context>

<tasks>
<task type="auto">
<name>Task 1: Add conviction threshold to database and storage</name>
<files>shared/schema.ts, server/storage.ts, bot/db.py</files>
<action>
Add conviction threshold configuration to the database layer.

In shared/schema.ts:
- Already has bot_state table with key/value storage
- No schema changes needed (use existing bot_state)

In server/storage.ts:
1. Add to BotConfig interface:
   - conviction_threshold: number (default 0)

2. Update config initialization:
   - Add conviction_threshold from env: CONVICTION_THRESHOLD
   - Default to 0 (show all signals)
   - Parse as float

3. Add methods to IStorage interface:
   - getConvictionThreshold(): number
   - setConvictionThreshold(value: number): void

4. Implement in SQLiteStorage:
   - Read from bot_state table (key: "conviction_threshold")
   - Store as JSON in bot_state
   - Return 0 if not set

In bot/db.py:
1. Add get_conviction_threshold() function:
   - Query bot_state for "conviction_threshold" key
   - Return float value or 0.0 default
   - Return type: float

2. Add set_conviction_threshold(value: float) function:
   - Store in bot_state table
   - Use set_bot_state helper

3. Seed default value in init_db() if not present
</action>
<verify>npm run check passes, Python functions work correctly</verify>
<done>BotConfig includes conviction_threshold, storage methods implemented, Python functions added</done>
</task>

<task type="auto">
<name>Task 2: Create threshold update API endpoint</name>
<files>server/routes.ts</files>
<action>
Add API endpoint to update conviction threshold from the dashboard.

In server/routes.ts:

1. Add GET /api/bot/threshold endpoint:
   - Returns current threshold value
   - Response: { conviction_threshold: number }
   - Read from storage.getConfig()

2. Add POST /api/bot/threshold endpoint:
   - Accepts body: { conviction_threshold: number }
   - Validates value is between 0 and 100
   - Updates storage.updateConfig({ conviction_threshold })
   - Broadcasts update via WebSocket (if available)
   - Returns: { conviction_threshold, status: "ok" }
   - On validation error: 400 with { error: "Invalid threshold" }

3. Update /api/bot/status to include:
   - conviction_threshold in the rules object

4. Follow existing error handling patterns (try/catch, proper JSON responses)

The threshold controls:
- Dashboard: Only show approvals with score >= threshold
- Bot: Only request approval for signals with score >= threshold
</action>
<verify>curl -X POST -H "Content-Type: application/json" -d '{"conviction_threshold":70}' http://localhost:5000/api/bot/threshold returns updated value</verify>
<done>GET and POST /api/bot/threshold endpoints created, included in status response</done>
</task>

<task type="auto">
<name>Task 3: Add conviction threshold slider to RulesPanel</name>
<files>client/src/components/RulesPanel.tsx</files>
<action>
Add an interactive conviction threshold slider to the RulesPanel component.

Current RulesPanel shows trading rules (max_trade_pct, daily_loss_cap, etc.).

Add:

1. Import Slider component:
   - import { Slider } from "@/components/ui/slider"
   - Import useMutation, useQueryClient from "@tanstack/react-query"
   - Import apiRequest from "@/lib/queryClient"

2. Add state for threshold:
   - const [threshold, setThreshold] = useState(0)
   - Initialize from props.rules.conviction_threshold or 0

3. Create mutation for updating threshold:
   - Use useMutation with POST /api/bot/threshold
   - On success, invalidate "/api/bot/status" query

4. Add slider UI:
   - Label: "Conviction Threshold"
   - Subtitle: "Only show signals scoring above this threshold"
   - Slider range: 0-100
   - Step: 5 (or 1 for fine control)
   - Display current value: "{threshold}/100"
   - Color: primary color for track

5. Add debounced update:
   - Update local state immediately
   - Debounce API call by 500ms
   - Show loading state while updating

6. Position in RulesPanel:
   - Add below existing rules
   - Use same card styling as other rules
   - Add separator above threshold section

Style requirements:
- Match existing card styling
- Show value next to slider
- Use appropriate spacing
</action>
<verify>RulesPanel shows conviction threshold slider, moving it updates value and calls API</verify>
<done>RulesPanel displays threshold slider (0-100), updates backend, debounced, styled consistently</done>
</task>

<task type="auto">
<name>Task 4: Filter approval queue by threshold</name>
<files>client/src/components/ApprovalQueue.tsx, bot/approval_gate.py</files>
<action>
Apply the conviction threshold to filter which signals appear.

In client/src/components/ApprovalQueue.tsx:
1. Accept threshold prop or read from data
2. Filter approvals to only show those with score >= threshold
3. Show count of hidden approvals if any are filtered out
4. Add tooltip or text: "Showing X of Y signals (filtered by threshold)"

In bot/approval_gate.py:
1. Import get_conviction_threshold from db
2. In request_approval(), before sending to Telegram:
   - Check if signal["score"] >= get_conviction_threshold()
   - If below threshold, log and skip (don't request approval)
   - Log message: "Signal below threshold, skipping"

3. Make sure the check happens early (before writing to pending_approvals)

This ensures:
- Dashboard only shows qualifying signals
- Bot only requests approval for qualifying signals
- User can adjust threshold and see immediate effect
</action>
<verify>Only approvals with score >= threshold appear in queue, bot skips low-score signals</verify>
<done>ApprovalQueue filters by threshold, bot respects threshold when creating approvals</done>
</task>
</tasks>

<verification>
1. RulesPanel shows conviction threshold slider (0-100)
2. Moving slider updates threshold in backend
3. Dashboard only shows approvals with score >= threshold
4. Hidden count shown if signals are filtered
5. Bot only requests approval for signals above threshold
6. Threshold persists across page reloads
</verification>

<success_criteria>
- Conviction threshold slider visible in RulesPanel
- Slider value updates threshold in database
- Dashboard filters approvals by threshold (score >= threshold)
- Bot only surfaces signals above threshold
- Threshold persists and applies immediately
- UI indicates when signals are hidden
</success_criteria>

<output>
After completion, create `.planning/phases/02-dashboard-completion/02-04-SUMMARY.md`
</output>
