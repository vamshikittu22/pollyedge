---
phase: 03-research-engine-polish
plan: 03
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified:
  - bot/market_scanner.py
autonomous: true

must_haves:
  truths:
    - "Market scanner provides agent-specific market discovery by tag"
    - "Scanner caches results for 30 seconds to avoid rate limits"
    - "All agents can use scanner to get relevant markets"
  artifacts:
    - path: "bot/market_scanner.py"
      contains: "get_markets_for_agent"
      min_lines: 80
  key_links:
    - from: "bot/market_scanner.py"
      to: "bot/agents/momentum_agent.py"
      via: "import and use in _get_active_markets"
---

<objective>
Create Polymarket market discovery scanner with agent-specific filtering.

Purpose: Provide a centralized market discovery module that allows each agent to get relevant markets based on their type (earnings, crypto, news, momentum, arb).
Output: market_scanner.py with get_markets_for_agent() function.
</objective>

<execution_context>
@C:/Users/kittu/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/kittu/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-research-engine-polish/03-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create market_scanner.py module</name>
  <files>bot/market_scanner.py</files>
  <action>
    Create a new file bot/market_scanner.py with:
    
    1. GAMMA_API constant = "https://gamma-api.polymarket.com"
    2. A cache dict with 30-second TTL
    3. get_markets_for_agent(agent_type: str, limit: int = 50) -> list function
    
    The function should return relevant markets based on agent_type:
    - "earnings": tag="earnings"
    - "crypto": tag="crypto"
    - "news": all active markets (limit 100)
    - "momentum": all active markets (limit 50)
    - "arb": tag="crypto,politics"
    - default: all active markets
    
    Use cache to avoid hitting rate limits. Cache key = agent_type.
  </action>
  <verify>ls -la bot/market_scanner.py</verify>
  <done>market_scanner.py created with get_markets_for_agent function</done>
</task>

<task type="auto">
  <name>Task 2: Update MomentumAgent to use market scanner</name>
  <files>bot/agents/momentum_agent.py</files>
  <action>
    In momentum_agent.py:
    
    1. Add import at top: from bot.market_scanner import get_markets_for_agent
    2. Replace _get_active_markets() call in scan() with: get_markets_for_agent("momentum")
    3. Remove the _get_active_markets method from the class
    
    This centralizes market discovery and uses caching.
  </action>
  <verify>grep -n "get_markets_for_agent\|from bot.market_scanner" bot/agents/momentum_agent.py</verify>
  <done>MomentumAgent uses centralized market scanner</done>
</task>

</tasks>

<verification>
- [ ] market_scanner.py imports without errors
- [ ] get_markets_for_agent("earnings") returns markets with earnings tag
- [ ] Cache prevents duplicate API calls within 30 seconds
</verification>

<success_crates>
Agents can discover relevant markets efficiently with caching to avoid rate limits.
</success_crates>

<output>
After completion, create `.planning/phases/03-research-engine-polish/03-03-SUMMARY.md`
</output>