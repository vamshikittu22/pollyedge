---
phase: 00-critical-fixes
plan: 04
type: execute
wave: 2
depends_on: [1, 2, 3]
files_modified:
  - bot/position_monitor.py
  - bot/pollyedge_bot.py
autonomous: true

must_haves:
  truths:
    - "Positions close automatically when profit target or stop loss is hit"
    - "P&L is logged at close time with actual value (not 0.0)"
    - "Telegram notification sent when position closes"
    - "bot_state.json reflects closed positions"
  artifacts:
    - path: "bot/position_monitor.py"
      provides: "Daemon thread that monitors open positions and exits them"
      min_lines: 80
  key_links:
    - from: "bot/position_monitor.py"
      to: "bot/risk_manager.py"
      via: "rm.record_trade_close()"
      pattern: "record_trade_close"
    - from: "bot/position_monitor.py"
      to: "bot_state.json"
      via: "load_state / save_state"
      pattern: "bot_state"
    - from: "bot/position_monitor.py"
      to: "logs/trades.csv"
      via: "log_trade()"
      pattern: "log_trade"
---

<objective>
Create position_monitor.py daemon thread that checks open positions every 30s and exits them when PROFIT_TARGET or STOP_LOSS is hit. Also fix log_trade() to be called at close (not open) with actual P&L.
</objective>

<context>
@.planning/ROADMAP.md
@C:\Users\kittu\Downloads\GIT\Pollyedge\pollyedge\bot\orchestrator.py
@C:\Users\kittu\Downloads\GIT\Pollyedge\pollyedge\bot\risk_manager.py
@C:\Users\kittu\Downloads\GIT\Pollyedge\pollyedge\bot\logger.py
</context>

<tasks>

<task type="auto">
  <name>Create position_monitor.py with exit logic</name>
  <files>bot/position_monitor.py</files>
  <action>
    Create `bot/position_monitor.py`:

    1. Import: `os, time, logging, requests, threading` + project modules
    2. `load_dotenv()` at top
    3. Constants from env: `PROFIT_TARGET` (default 0.30 = +30%), `STOP_LOSS` (default 0.10 = -10%)
    4. `CLOB_API = "https://clob.polymarket.com"`
    5. `monitor_positions(dry_run: bool = True)` — infinite loop:
       - Load state from `bot_state.json`
       - Calculate live balance
       - For each open position in `state["open_positions"]`:
         - Fetch current midpoint price via `requests.get(f"{CLOB_API}/midpoint", params={"token_id": token_id})`
         - Calculate PnL %: for BUY = (exit - entry) / entry, for SELL = (entry - exit) / entry
         - If `pnl_pct >= PROFIT_TARGET` → close as "PROFIT TARGET"
         - If `pnl_pct <= -STOP_LOSS` → close as "STOP LOSS"
         - Call `rm.record_trade_close(token_id, exit_price)` to record exit
         - Call `log_trade()` with actual P&L value (not 0.0)
         - Send Telegram notification with emoji (💰 for profit, 🔴 for loss)
       - Sleep 30 seconds between checks
    6. `daemon=True` in the thread — exits when main process exits

    Key: P&L MUST be calculated and passed to log_trade() at close time, not entry time.
  </action>
  <verify>python -c "from bot.position_monitor import monitor_positions; print('monitor_positions function exists')"</verify>
  <done>Positions automatically close at profit target or stop loss</done>
</task>

<task type="auto">
  <name>Wire position monitor into pollyedge_bot.py</name>
  <files>bot/pollyedge_bot.py</files>
  <action>
    In `bot/pollyedge_bot.py`:

    1. Add import: `from bot.position_monitor import monitor_positions`
    2. Add threading import: `import threading`
    3. After the signal.signal() calls, start the monitor thread:
       ```python
       monitor_thread = threading.Thread(
           target=monitor_positions, args=(DRY_RUN,), daemon=True, name="PositionMonitor"
       )
       monitor_thread.start()
       ```

    Also: REMOVE the `log_trade(token, label, price, 0.0)` call from `orchestrator._execute()` — let the position monitor call it at close with the real P&L.
  </action>
  <verify>grep -n "monitor_thread" bot/pollyedge_bot.py</verify>
  <done>Position monitor runs as daemon thread from bot startup</done>
</task>

<task type="auto">
  <name>Remove log_trade call from orchestrator._execute</name>
  <files>bot/orchestrator.py</files>
  <action>
    In `bot/orchestrator.py`, find where `log_trade(token, label, price, 0.0)` is called (around line 178). Remove this line — the position monitor will call log_trade() at close time with the actual P&L.
  </action>
  <verify>grep -n "log_trade" bot/orchestrator.py</verify>
  <done>log_trade is only called at close with real P&L (not at open with 0.0)</done>
</task>

</tasks>

<verification>
1. Open a position manually in bot_state.json
2. Start the bot — position monitor thread should be visible in logs
3. Wait 30s — check if position was closed based on thresholds
4. Verify trades.csv has entry with non-zero P&L
5. Verify Telegram notification was sent
</verification>

<success_criteria>
- Position monitor daemon thread starts with the bot
- Positions close when profit target (+30%) or stop loss (-10%) is hit
- log_trade() logs actual P&L at close (not 0.0)
- Telegram notification sent on close with P&L details
- bot_state.json no longer contains closed positions
</success_criteria>

<output>
After completion, create `.planning/phases/00-critical-fixes/00-04-SUMMARY.md`
</output>
