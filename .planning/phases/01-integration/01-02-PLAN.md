---
phase: 01-integration
plan: 02
type: execute
wave: 2
depends_on: [1]
files_modified:
  - bot/db.py
  - bot/risk_manager.py
  - bot/pollyedge_bot.py
autonomous: true

must_haves:
  truths:
    - "Python code reads/writes bot state from SQLite instead of JSON"
    - "Bot state survives process restarts via SQLite"
    - "Position open/close operations use database transactions"
  artifacts:
    - path: "bot/db.py"
      provides: "SQLite database module for Python"
      min_lines: 60
  key_links:
    - from: "bot/risk_manager.py"
      to: "bot/db.py"
      via: "import + function calls"
      pattern: "from bot.db import"
    - from: "bot/db.py"
      to: "./data/pollyedge.db"
      via: "sqlite3.connect"
      pattern: "sqlite3"
---

<objective>
Create bot/db.py as the Python SQLite database layer, then migrate risk_manager.py to use it instead of JSON file operations. This is the foundation for all Python → SQLite migrations.
</objective>

<context>
@.planning/ROADMAP.md
@C:\Users\kittu\Downloads\GIT\Pollyedge\pollyedge\bot\risk_manager.py
@C:\Users\kittu\Downloads\GIT\Pollyedge\pollyedge\bot\position_monitor.py
@C:\Users\kittu\Downloads\GIT\Pollyedge\pollyedge\bot\orchestrator.py
</context>

<tasks>

<task type="auto">
  <name>Create bot/db.py SQLite database module</name>
  <files>bot/db.py</files>
  <action>
    Create `bot/db.py` with SQLite database operations:

    1. Use `import sqlite3` (stdlib, no new dependency)
    2. `DB_PATH = "data/pollyedge.db"` (same path as TypeScript)
    3. `get_db()` — returns connection with row_factory=dict_factory
    4. Key functions:
       - `get_bot_state()` — returns dict with daily_pnl, daily_date, total_trades, all_time_pnl, bot_active
       - `set_bot_state(key, value)` — upsert into bot_state table
       - `get_open_positions()` — returns dict keyed by token_id
       - `add_position(token_id, side, size, entry_price, label)` — insert into open_positions
       - `remove_position(token_id)` — delete from open_positions
       - `get_config()` — return config dict from bot_state
       - `set_config(key, value)` — update bot_state
    5. Ensure data/ directory exists
    6. Create tables if they don't exist (initialize on first run)

    Thread safety: Use connection per operation (sqlite3 is thread-safe for connections, but use locks for write operations).
  </action>
  <verify>py -c "from bot.db import get_db, get_bot_state; print('bot/db.py OK:', type(get_bot_state()))"</verify>
  <done>Python has a clean SQLite database module</done>
</task>

<task type="auto">
  <name>Migrate risk_manager.py to use SQLite</name>
  <files>bot/risk_manager.py</files>
  <action>
    Update `bot/risk_manager.py` to use `bot.db` instead of JSON file operations:

    1. Remove: `import json`, `STATE_FILE`, `load_state()`, `save_state()`
    2. Add: `from bot.db import get_db, get_open_positions, add_position, remove_position, get_bot_state, set_bot_state`
    3. In `RiskManager.__init__`: Call `get_bot_state()` and `get_open_positions()` instead of `load_state()`
    4. In `reset_daily_if_needed()`: Use `set_bot_state()` instead of `save_state()`
    5. In `record_trade_open()`: Use `add_position()` instead of dict manipulation + save
    6. In `record_trade_close()`: Use `remove_position()` and `set_bot_state()` instead of dict manipulation + save
    7. In `kill_switch()`: Use `set_bot_state()` instead of `save_state()`
    8. Remove ALL `save_state(self.state)` calls — db handles persistence

    IMPORTANT: The open_positions are stored in a separate table. The bot_state table stores scalars.
    Make sure the data model matches the schema in shared/schema.ts.
  </action>
  <verify>py -c "from bot.risk_manager import RiskManager; rm = RiskManager(10); print('bot_active:', rm.state.get('bot_active')); print('open_positions:', len(rm.state.get('open_positions', {})))"</verify>
  <done>risk_manager.py uses SQLite instead of JSON files</done>
</task>

</tasks>

<verification>
1. Start bot — bot_state table has entries
2. Open a position via the bot
3. Check `data/pollyedge.db` — open_positions table has the position
4. Restart bot — position still exists (persisted)
5. Close position — position removed from DB
</verification>

<success_criteria>
- bot/db.py provides clean SQLite interface
- risk_manager.py uses SQLite (no JSON file reads/writes)
- Bot state persists across restarts
- Open positions persist across restarts
- No regressions in risk rule logic
</success_criteria>

<output>
After completion, create `.planning/phases/01-integration/01-02-SUMMARY.md`
</output>
