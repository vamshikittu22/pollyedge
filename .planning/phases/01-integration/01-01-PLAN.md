---
phase: 01-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - drizzle.config.ts
  - shared/schema.ts
autonomous: true

must_haves:
  truths:
    - "SQLite database is configured and migrations can run"
    - "Schema covers all 4 data types (bot_state, agents, approvals, trades)"
    - "Drizzle schema can generate TypeScript types for TypeScript side"
  artifacts:
    - path: "shared/schema.ts"
      provides: "Drizzle ORM schema for all tables"
      min_lines: 50
    - path: "drizzle.config.ts"
      provides: "SQLite configuration"
  key_links:
    - from: "drizzle.config.ts"
      to: "shared/schema.ts"
      via: "schema import"
      pattern: "schema:"
    - from: "shared/schema.ts"
      to: "Python side"
      via: "TypeScript types shared"
      pattern: "drizzle-orm"
---

<objective>
Configure SQLite for Drizzle ORM and define the unified schema covering all 4 data types. This is the foundation for all subsequent migration plans.
</objective>

<context>
@.planning/ROADMAP.md
@C:\Users\kittu\Downloads\GIT\Pollyedge\pollyedge\drizzle.config.ts
@C:\Users\ittu\Downloads\GIT\Pollyedge\pollyedge\server\storage.ts
@C:\Users\kittu\Downloads\GIT\Pollyedge\pollyedge\bot\risk_manager.py
@C:\Users\kittu\Downloads\GIT\Pollyedge\pollyedge\bot\logger.py
@C:\Users\kittu\Downloads\GIT\Pollyedge\pollyedge\bot\agents\base_agent.py
</context>

<tasks>

<task type="auto">
  <name>Configure Drizzle for SQLite</name>
  <files>drizzle.config.ts</files>
  <action>
    Update `drizzle.config.ts` to use SQLite instead of PostgreSQL:

    1. Add `better-sqlite3` package: `npm install better-sqlite3 && npm install -D @types/better-sqlite3 drizzle-kit`
    2. Change dialect to "sqlite"
    3. Use `better-sqlite3` driver
    4. Database file: `./data/pollyedge.db` (create data/ directory)
    5. Keep schema import pointing to `shared/schema.ts`

    Final config should look like:
    ```typescript
    import { defineConfig } from "drizzle-kit";
    import Database from "better-sqlite3";
    import * as schema from "./shared/schema";

    export default defineConfig({
      out: "./migrations",
      schema: "./shared/schema.ts",
      dialect: "sqlite",
      dbCredentials: {
        url: "./data/pollyedge.db",
      },
    });
    ```

    NOTE: Remove the DATABASE_URL check — SQLite doesn't need it.
  </action>
  <verify>npm run db:generate 2>&1 | head -20</verify>
  <done>Drizzle configured for SQLite with working schema</done>
</task>

<task type="auto">
  <name>Define unified schema in shared/schema.ts</name>
  <files>shared/schema.ts</files>
  <action>
    Create or update `shared/schema.ts` with Drizzle schema for all 4 data types:

    1. **Table: bot_state** — single row for bot state
       - `key` TEXT PRIMARY KEY (e.g., "daily_pnl", "all_time_pnl")
       - `value` TEXT (JSON-encoded for complex values)

    2. **Table: open_positions** — one row per open position
       - `token_id` TEXT PRIMARY KEY
       - `side` TEXT ("BUY" or "SELL")
       - `size` REAL
       - `entry_price` REAL
       - `label` TEXT
       - `opened_at` TEXT (ISO timestamp)

    3. **Table: agent_status** — one row per agent
       - `name` TEXT PRIMARY KEY
       - `status` TEXT ("running" | "stopped" | "error")
       - `last_scan` TEXT (ISO timestamp)
       - `signals_found` INTEGER

    4. **Table: pending_approvals** — one row per approval request
       - `id` TEXT PRIMARY KEY
       - `label` TEXT
       - `side` TEXT
       - `size` REAL
       - `edge` REAL
       - `source` TEXT
       - `score` INTEGER
       - `market_prob` REAL
       - `model_prob` REAL
       - `timestamp` TEXT (ISO)
       - `status` TEXT ("pending" | "approved" | "rejected" | "expired")

    5. **Table: trades** — one row per closed trade
       - `id` INTEGER PRIMARY KEY AUTOINCREMENT
       - `token_id` TEXT
       - `label` TEXT
       - `exit_price` REAL
       - `pnl` REAL
       - `closed_at` TEXT (ISO timestamp)

    Export all tables and a `db` instance for use in the TypeScript server.
    Also export TypeScript types matching each table's shape.
  </action>
  <verify>ls shared/schema.ts && grep -c "sqliteTable" shared/schema.ts</verify>
  <done>Unified schema covers all 4 data types with TypeScript types exported</done>
</task>

<task type="auto">
  <name>Create data directory and generate initial migration</name>
  <files>
    - data/.gitkeep
    - migrations/0001_init.sql
  </files>
  <action>
    1. Create `data/` directory with `.gitkeep` (so the folder is tracked)
    2. Run `npx drizzle-kit generate` to create migration files
    3. Run `npx drizzle-kit migrate` to apply migration to ./data/pollyedge.db
    4. Verify the SQLite database was created with all 5 tables
    5. Add `data/*.db` to .gitignore if not already there

    Use Python's sqlite3 for the initial migration (or npm scripts if drizzle-kit works):
    ```bash
    npm install better-sqlite3
    npx drizzle-kit generate
    npx drizzle-kit migrate
    ```
  </action>
  <verify>ls data/ && sqlite3 data/pollyedge.db ".tables"</verify>
  <done>SQLite database created with all 5 tables</done>
</task>

</tasks>

<verification>
1. `npx drizzle-kit generate` creates migration files
2. `npx drizzle-kit migrate` creates ./data/pollyedge.db
3. All 5 tables exist: bot_state, open_positions, agent_status, pending_approvals, trades
4. TypeScript types are exported from shared/schema.ts
</verification>

<success_criteria>
- Drizzle configured for SQLite (not PostgreSQL)
- All 5 tables defined in shared/schema.ts
- SQLite database created at ./data/pollyedge.db
- Migration files generated in ./migrations/
- TypeScript types exported for all tables
- .gitignore updated to exclude .db files
</success_criteria>

<output>
After completion, create `.planning/phases/01-integration/01-01-SUMMARY.md`
</output>
