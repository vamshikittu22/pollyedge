---
phase: 03-research-engine-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - bot/agents/momentum_agent.py
autonomous: true

must_haves:
  truths:
    - "MomentumAgent signals have lower threshold for more opportunities"
    - "MomentumAgent includes structured analysis breakdown"
    - "Price history uses volume-weighted average for more accurate signals"
  artifacts:
    - path: "bot/agents/momentum_agent.py"
      contains: "analysis_breakdown"
      min_lines: 100
  key_links:
    - from: "bot/agents/momentum_agent.py"
      to: "bot/orchestrator.py"
      via: "signal dict with analysis_breakdown field"
---

<objective>
Improve MomentumAgent with lower threshold, volume-weighted average, and structured analysis breakdown.

Purpose: Generate more signals (lower 8% to 5% threshold) while improving signal quality with structured analysis fields.
Output: Enhanced MomentumAgent with analysis_breakdown and improved scoring potential.
</objective>

<execution_context>
@C:/Users/kittu/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/kittu/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-research-engine-polish/03-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Lower EDGE_THRESHOLD to 5%</name>
  <files>bot/agents/momentum_agent.py</files>
  <action>
    Change line 20 from:
    EDGE_THRESHOLD = float(os.getenv("EDGE_THRESHOLD", 0.08))
    to:
    EDGE_THRESHOLD = float(os.getenv("EDGE_THRESHOLD", 0.05))
    
    Update comment on line 18 to reflect new default.
  </action>
  <verify>grep -n "EDGE_THRESHOLD.*0.05" bot/agents/momentum_agent.py</verify>
  <done>Default threshold is now 5% instead of 8%</done>
</task>

<task type="auto">
  <name>Task 2: Add volume-weighted average to price history</name>
  <files>bot/agents/momentum_agent.py</files>
  <action>
    Add a helper method _weighted_avg() that weights recent prices more heavily:
    - Most recent price: weight 3
    - Second most recent: weight 2  
    - All others: weight 1
    
    Modify scan() to use _weighted_avg() instead of simple average when calculating model_prob and edge.
    
    Keep simple average as fallback if less than 3 data points.
  </action>
  <verify>grep -n "weighted_avg\|weight" bot/agents/momentum_agent.py | head -10</verify>
  <done>Price calculations use weighted average favoring recent prices</done>
</task>

<task type="auto">
  <name>Task 3: Add structured analysis_breakdown to signals</name>
  <files>bot/agents/momentum_agent.py</files>
  <action>
    Update the signal dict in scan() to include analysis_breakdown:
    
    signals.append({
        "token_id": tok,
        "side": side,
        "market_prob": mid,
        "model_prob": round(model_p, 4),
        "edge": round(edge, 4),
        "label": f"MR:{label}",
        "source": "momentum",
        "analysis": f"Deviation: {dev:+.2%} from {WINDOW_SIZE}hr avg",
        "volume": float(mkt.get("volume", 0) or 0),
        "analysis_breakdown": {
            "current_price": round(mid, 4),
            "rolling_avg": round(avg, 4),
            "deviation": f"{dev:+.2%}",
            "window_size": WINDOW_SIZE,
            "direction": side,
            "trend": "oversold" if dev < 0 else "overbought",
            "weighting": "volume-weighted" if len(history) >= 3 else "simple"
        }
    })
  </action>
  <verify>grep -n "analysis_breakdown" bot/agents/momentum_agent.py</verify>
  <done>Signals include analysis_breakdown with current_price, rolling_avg, deviation, direction, trend</done>
</task>

</tasks>

<verification>
- [ ] MomentumAgent loads without errors (python -c "from bot.agents.momentum_agent import MomentumAgent; print('OK')")
- [ ] EDGE_THRESHOLD defaults to 0.05
- [ ] Signals contain analysis_breakdown field
</verification>

<success_criteria>
MomentumAgent generates signals with structured analysis breakdown, enabling higher confidence scores in orchestrator.
</success_criteria>

<output>
After completion, create `.planning/phases/03-research-engine-polish/03-01-SUMMARY.md`
</output>