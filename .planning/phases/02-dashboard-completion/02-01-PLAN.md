---
phase: 02-dashboard-completion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - client/src/components/ApprovalQueue.tsx
  - server/routes.ts
  - server/storage.ts
  - shared/schema.ts
autonomous: true
must_haves:
  truths:
    - "User sees approve button on pending approval cards"
    - "User sees reject button on pending approval cards"
    - "Clicking approve updates approval status to 'approved' in database"
    - "Clicking reject updates approval status to 'rejected' in database"
    - "UI immediately reflects status change after button click"
    - "Bot receives approval/rejection signal (via database)"
  artifacts:
    - path: "client/src/components/ApprovalQueue.tsx"
      provides: "Approve/reject buttons with mutation handling"
      exports: ["ApprovalQueue"]
    - path: "server/routes.ts"
      provides: "POST /api/approvals/:id/approve and /reject endpoints"
      exports: ["registerRoutes"]
    - path: "server/storage.ts"
      provides: "resolveApproval method to update approval status"
      exports: ["IStorage", "SQLiteStorage"]
  key_links:
    - from: "ApprovalQueue.tsx"
      to: "POST /api/approvals/:id/approve"
      via: "apiRequest mutation"
      pattern: "mutationFn:.*approve"
    - from: "routes.ts approve endpoint"
      to: "storage.resolveApproval"
      via: "function call"
      pattern: "storage\.resolveApproval"
    - from: "storage.resolveApproval"
      to: "pending_approvals table"
      via: "UPDATE statement"
      pattern: "UPDATE pending_approvals"
---

<objective>
Add approve/reject buttons to the dashboard approval queue, with API endpoints that update the database and notify the bot.

**Purpose:** Enable users to approve or reject trades directly from the dashboard without needing Telegram.

**Output:** Working approve/reject flow from UI through database.
</objective>

<execution_context>
@C:/Users/kittu/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/kittu/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@client/src/components/ApprovalQueue.tsx
@server/routes.ts
@server/storage.ts
@shared/schema.ts
@client/src/lib/queryClient.ts
@bot/db.py
@bot/approval_gate.py
</context>

<tasks>
<task type="auto">
<name>Task 1: Add resolveApproval method to storage.ts</name>
<files>server/storage.ts</files>
<action>
Add a `resolveApproval(id: string, status: "approved" | "rejected")` method to the `IStorage` interface and `SQLiteStorage` class.

The method should:
1. Update the pending_approvals table in SQLite
2. Set status to the provided value for the given id
3. Return nothing (void)

Use Drizzle ORM with eq() for the WHERE clause. Follow the pattern used in existing methods like getPendingApprovals().

Also add TypeScript type definition to the IStorage interface.
</action>
<verify>npm run check passes with no TypeScript errors</verify>
<done>IStorage interface includes resolveApproval method signature, SQLiteStorage implements it with Drizzle update query</done>
</task>

<task type="auto">
<name>Task 2: Create approve/reject API endpoints</name>
<files>server/routes.ts</files>
<action>
Add two POST endpoints to registerRoutes:

1. POST /api/approvals/:id/approve
   - Extract id from req.params
   - Call storage.resolveApproval(id, "approved")
   - Return JSON: { id, status: "approved" }

2. POST /api/approvals/:id/reject  
   - Extract id from req.params
   - Call storage.resolveApproval(id, "rejected")
   - Return JSON: { id, status: "rejected" }

Both endpoints should:
- Use async/await
- Handle errors with try/catch
- Return 500 on error with JSON { error: message }

Follow the pattern of the existing /api/bot/toggle endpoint.
</action>
<verify>curl -X POST http://localhost:5000/api/approvals/test-id/approve returns JSON response</verify>
<done>Both endpoints registered, call storage.resolveApproval, return proper JSON responses</done>
</task>

<task type="auto">
<name>Task 3: Add approve/reject buttons to ApprovalQueue component</name>
<files>client/src/components/ApprovalQueue.tsx</files>
<action>
Modify the ApprovalQueue component to show approve/reject buttons for pending approvals.

Changes needed:
1. Import { useMutation, useQueryClient } from "@tanstack/react-query"
2. Import { apiRequest } from "@/lib/queryClient"
3. Import { Button } from "@/components/ui/button"
4. Import { Check, X } from "lucide-react"

5. Inside the component, add mutation hooks:
   - approveMutation: POST to /api/approvals/:id/approve
   - rejectMutation: POST to /api/approvals/:id/reject
   - On success, invalidate "/api/bot/status" query to refresh data

6. For each pending approval (status === "pending"), add:
   - Approve button (green, with Check icon) on the left
   - Reject button (red, with X icon) on the right
   - Buttons should be small size, side by side below the approval details
   - Disable buttons while mutation is pending

7. For approved/rejected/expired approvals, show the status badge (already exists) but no buttons

Use the existing toast hook for success/error notifications.
</action>
<verify>Dashboard displays approve/reject buttons for pending approvals; clicking them updates status</verify>
<done>ApprovalQueue shows buttons for pending items, mutations call API endpoints, UI updates after success</done>
</task>
</tasks>

<verification>
1. Dashboard loads and shows pending approvals with approve/reject buttons
2. Clicking approve sends POST to /api/approvals/:id/approve
3. Database updates approval status to "approved"
4. UI refreshes to show approved status (no buttons)
5. Same flow works for reject
</verification>

<success_criteria>
- Approve and reject buttons visible on pending approval cards
- Clicking approve updates status to "approved" in database
- Clicking reject updates status to "rejected" in database
- UI immediately reflects status change
- Bot can read updated status from database
</success_criteria>

<output>
After completion, create `.planning/phases/02-dashboard-completion/02-01-SUMMARY.md`
</output>
