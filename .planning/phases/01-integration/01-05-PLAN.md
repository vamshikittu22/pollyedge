---
phase: 01-integration
plan: 05
type: execute
wave: 2
depends_on: [1]
files_modified:
  - bot/logger.py
  - server/storage.ts
autonomous: true

must_haves:
  truths:
    - "Python writes closed trades to SQLite"
    - "TypeScript reads trades from SQLite"
    - "Dashboard shows trade history from SQLite (not CSV polling)"
  artifacts:
    - path: "bot/logger.py"
      provides: "Trade logging to SQLite"
    - path: "server/storage.ts"
      provides: "TypeScript reads trades from SQLite"
  key_links:
    - from: "bot/logger.py"
      to: "bot/db.py"
      via: "from bot.db import log_trade_to_db"
      pattern: "log_trade_to_db"
    - from: "server/storage.ts"
      to: "./shared/schema"
      via: "import * as schema from"
      pattern: "drizzle"
---

<objective>
Migrate logs/trades.csv to SQLite. Python writes closed trades to SQLite, TypeScript reads from the same database. Remove CSV polling from the dashboard.
</objective>

<context>
@.planning/ROADMAP.md
@C:\Users\kittu\Downloads\GIT\Pollyedge\pollyedge\bot\logger.py
@C:\Users\kittu\Downloads\GIT\Pollyedge\pollyedge\bot\db.py
@C:\Users\kittu\Downloads\GIT\Pollyedge\pollyedge\server\storage.ts
</context>

<tasks>

<task type="auto">
  <name>Add trade logging functions to bot/db.py</name>
  <files>bot/db.py</files>
  <action>
    Add to `bot/db.py`:

    1. `log_trade_to_db(token_id, label, exit_price, pnl)` — insert into trades table with timestamp
    2. `get_recent_trades(limit=50)` — select last N trades, ordered by closed_at desc
    3. `get_all_time_pnl()` — calculate sum of pnl from trades table
  </action>
  <verify>py -c "from bot.db import log_trade_to_db, get_recent_trades; print('trade logging functions OK')"</verify>
  <done>bot/db.py has trade logging functions</done>
</task>

<task type="auto">
  <name>Migrate bot/logger.py to use SQLite</name>
  <files>bot/logger.py</files>
  <action>
    Update `bot/logger.py`:

    1. Add: `from bot.db import log_trade_to_db`
    2. In `log_trade()`: Call `log_trade_to_db(token_id, label, exit_price, pnl)` instead of (or in addition to) CSV append
    3. Keep the CSV file as a backup/audit log if desired, but SQLite is primary
    4. `ensure_log_dir()` and `ensure_csv_header()` can be removed if not keeping CSV

    The API stays the same: `log_trade(token_id, label, exit_price, pnl)` — just change the implementation.
  </action>
  <verify>py -c "from bot.logger import log_trade; print('logger imports OK')"</verify>
  <done>bot/logger.py writes trades to SQLite</done>
</task>

<task type="auto">
  <name>Migrate server/storage.ts to read from SQLite</name>
  <files>server/storage.ts</files>
  <action>
    Update `server/storage.ts`:

    1. Replace `MemStorage` with a SQLite-backed implementation
    2. Import from `drizzle-orm` and `better-sqlite3`
    3. Import schema from `../shared/schema`
    4. Replace `getTrades()`: query SQLite trades table (last 50), not CSV file
    5. Replace `getAgentStatus()`: query SQLite agent_status table, not JSON file
    6. Replace `getPendingApprovals()`: query SQLite pending_approvals table, not JSON file
    7. Replace `getBotState()`: query SQLite bot_state + open_positions tables, not JSON file
    8. `saveBotState()`: write to SQLite instead of JSON

    The interface `IStorage` stays the same — only the implementation changes.
    Keep `MemStorage` class name for backward compatibility.
  </action>
  <verify>npm run build 2>&1 | grep -E "(error|✓|success)" | head -10</verify>
  <done>server/storage.ts reads from SQLite, not JSON files</done>
</task>

</tasks>

<verification>
1. Position closes (via position monitor)
2. Check `data/pollyedge.db` — trades table has the entry
3. Dashboard refreshes — TradesTable shows the trade from SQLite
4. CSV file is no longer the primary data source
5. Bot restart — trade history persists from SQLite
</verification>

<success_criteria>
- bot/logger.py writes to SQLite
- server/storage.ts reads from SQLite (IStorage interface unchanged)
- Dashboard shows trade history from SQLite (not CSV)
- Trade history persists across bot restarts
- CSV file can be kept as backup but is not primary
</success_criteria>

<output>
After completion, create `.planning/phases/01-integration/01-05-SUMMARY.md`
</output>
