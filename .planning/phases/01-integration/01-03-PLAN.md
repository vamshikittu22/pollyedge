---
phase: 01-integration
plan: 03
type: execute
wave: 2
depends_on: [1]
files_modified:
  - bot/agents/base_agent.py
autonomous: true

must_haves:
  truths:
    - "Python agents write status to SQLite instead of agent_status.json"
    - "Dashboard can read agent status from SQLite via TypeScript"
    - "Both Python and TypeScript read from the same database"
  artifacts:
    - path: "bot/agents/base_agent.py"
      provides: "Agent status writes to SQLite"
  key_links:
    - from: "bot/agents/base_agent.py"
      to: "bot/db.py"
      via: "from bot.db import update_agent_status"
      pattern: "update_agent_status"
---

<objective>
Migrate agent_status.json to SQLite. Both Python agents and TypeScript server will read from the same SQLite database.
</objective>

<context>
@.planning/ROADMAP.md
@C:\Users\kittu\Downloads\GIT\Pollyedge\pollyedge\bot\agents\base_agent.py
@C:\Users\kittu\Downloads\GIT\Pollyedge\pollyedge\bot\db.py
</context>

<tasks>

<task type="auto">
  <name>Add agent status functions to bot/db.py</name>
  <files>bot/db.py</files>
  <action>
    Add to `bot/db.py`:

    1. `get_agent_status()` — returns list of agent dicts from agent_status table
    2. `update_agent_status(name, status, signals_found)` — upsert agent status
    3. These map to the `agent_status` table in the schema
  </action>
  <verify>py -c "from bot.db import update_agent_status; update_agent_status('TestAgent', 'running', 5); print('agent status OK')"</verify>
  <done>bot/db.py has agent status read/write functions</done>
</task>

<task type="auto">
  <name>Migrate base_agent.py to use SQLite</name>
  <files>bot/agents/base_agent.py</files>
  <action>
    Update `bot/agents/base_agent.py`:

    1. Remove: `import json`, `AGENTS_FILE`, `_file_lock`
    2. Remove: the entire `_write_status()` method (its logic moves to db.py)
    3. Add: `from bot.db import update_agent_status`
    4. Replace `_write_status(status, signals_found)` calls with `update_agent_status(self.name, status, signals_found)`
    5. Keep the method signature for compatibility, but change the body to just call db

    Example:
    ```python
    def _write_status(self, status: str, signals_found: int):
        """Write this agent's status to SQLite."""
        update_agent_status(self.name, status, signals_found)
    ```
  </action>
  <verify>py -c "from bot.agents.base_agent import BaseAgent; print('base_agent imports OK')"</verify>
  <done>Python agents write status to SQLite</done>
</task>

</tasks>

<verification>
1. Start the bot
2. Check `data/pollyedge.db` — agent_status table has entries for all 5 agents
3. Dashboard reads from same database — agent statuses show correctly
4. agent_status.json file is no longer created (or is ignored)
</verification>

<success_criteria>
- base_agent.py uses SQLite (no JSON file writes for agent status)
- agent_status.json is no longer written
- TypeScript server can read agent status from SQLite
- Dashboard shows live agent status from SQLite
</success_criteria>

<output>
After completion, create `.planning/phases/01-integration/01-03-SUMMARY.md`
</output>
