---
phase: 03-research-engine-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - bot/orchestrator.py
autonomous: true

must_haves:
  truths:
    - "Conviction scoring includes multi-factor weights (edge, source, confluence, volume)"
    - "Confluence detection adds +15 per cross-agent agreement"
    - "analysis_breakdown presence adds +5 to score"
    - "New formula produces scores 0-100 with clear differentiation"
  artifacts:
    - path: "bot/orchestrator.py"
      contains: "_score_signal"
      min_lines: 211
  key_links:
    - from: "bot/orchestrator.py"
      to: "bot/agents/momentum_agent.py"
      via: "analysis_breakdown field in signal dict"
---

<objective>
Enhance the conviction scoring algorithm with multi-factor weights.

Purpose: Replace basic scoring with improved formula that considers edge (0-40), source reliability (0-25), confluence (0-15), and volume (0-20) for better signal quality differentiation.
Output: Updated _score_signal method in orchestrator.py.
</objective>

<execution_context>
@C:/Users/kittu/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/kittu/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-research-engine-polish/03-RESEARCH.md
@.planning/phases/03-research-engine-polish/03-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update _score_signal with improved multi-factor formula</name>
  <files>bot/orchestrator.py</files>
  <action>
    Replace the existing _score_signal method (lines 144-179) with the improved version:
    
    def _score_signal(self, signal: dict) -> int:
        """Score 0-100 with multi-factor weighing."""
        score = 0
        edge = abs(signal.get("edge", 0))

        # 1. Edge score (0-40)
        if edge >= 0.25:
            score += 40
        elif edge >= 0.18:
            score += 30
        elif edge >= 0.12:
            score += 20
        elif edge >= 0.08:
            score += 10

        # 2. Source reliability (0-25)
        source_scores = {
            "earnings": 25,      # Multi-source validation
            "news+momentum": 20, # Cross-agent
            "news": 18,
            "momentum": 15,
            "crypto": 12,
            "arb": 10,
            "underpriced": 10,
        }
        score += source_scores.get(signal.get("source", ""), 10)

        # 3. Confluence (0-15) - NEW
        confirmations = signal.get("confirmations", 1)
        score += min((confirmations - 1) * 15, 15)
        
        # Boost for structured analysis breakdown
        if signal.get("analysis_breakdown"):
            score += 5

        # 4. Volume (0-20)
        vol = signal.get("volume", 0)
        if vol > 1_000_000:
            score += 20
        elif vol > 500_000:
            score += 15
        elif vol > 100_000:
            score += 10

        return min(score, 100)
  </action>
  <verify>grep -n "analysis_breakdown" bot/orchestrator.py</verify>
  <done>_score_signal uses multi-factor formula with analysis_breakdown boost</done>
</task>

</tasks>

<verification>
- [ ] Orchestrator imports without errors (python -c "from bot.orchestrator import Orchestrator; print('OK')")
- [ ] _score_signal handles signals without analysis_breakdown gracefully
- [ ] Score calculation caps at 100
</verification>

<success_criteria>
Conviction scoring produces differentiated scores 0-100 based on edge, source, confluence, and volume.
</success_criteria>

<output>
After completion, create `.planning/phases/03-research-engine-polish/03-02-SUMMARY.md`
</output>