---
phase: 03-research-engine-polish
plan: 04
type: execute
wave: 2
depends_on:
  - "03-01"
  - "03-02"
files_modified:
  - bot/approval_gate.py
  - shared/schema.ts
autonomous: true

must_haves:
  truths:
    - "Analysis breakdown stored in pending_approvals table"
    - "Frontend displays all analysis breakdown fields"
    - "Confidence score shown prominently for each signal"
  artifacts:
    - path: "bot/approval_gate.py"
      contains: "analysis_breakdown"
    - path: "shared/schema.ts"
      contains: "analysisBreakdown"
  key_links:
    - from: "bot/approval_gate.py"
      to: "bot/agents/momentum_agent.py"
      via: "signal dict with analysis_breakdown field"
    - from: "client/src/components/ApprovalQueue.tsx"
      to: "shared/schema.ts"
      via: "PendingApproval type + analysis parsing"
---

<objective>
Wire analysis_breakdown from Python agents through to dashboard display.

Purpose: Ensure structured analysis from agents (MomentumAgent, EarningsAgent, etc.) flows through approval_gate to pending_approvals table and displays on dashboard.
Output: Enhanced approval entries with full analysis breakdown visible in UI.
</objective>

<execution_context>
@C:/Users/kittu/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/kittu/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-research-engine-polish/03-RESEARCH.md
@.planning/phases/03-research-engine-polish/03-01-PLAN.md
@.planning/phases/03-research-engine-polish/03-02-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend pending_approvals schema with analysisBreakdown field</name>
  <files>shared/schema.ts</files>
  <action>
    Add analysisBreakdown column to pending_approvals table in shared/schema.ts:
    
    analysisBreakdown: text("analysis_breakdown"),  // JSON string for structured analysis
    
    Update the NewPendingApproval type to include it.
  </action>
  <verify>grep -n "analysis_breakdown" shared/schema.ts</verify>
  <done>Schema includes analysis_breakdown column</done>
</task>

<task type="auto">
  <name>Task 2: Update approval_gate to include analysis_breakdown in entry</name>
<files>bot/approval_gate.py</files>
<action>
    In approval_gate.py where entry dict is built (around line 120), add:
    
    "analysis_breakdown": json.dumps(signal.get("analysis_breakdown", {}))
    
    Update add_pending_approval call to include this field in the dict.
  </action>
<verify>grep -n "analysis_breakdown" bot/approval_gate.py</verify>
  <done>Approval entry includes analysis_breakdown from signal</done>
</task>

<task type="auto">
  <name>Task 3: Add db.py support for analysis_breakdown column</name></name>
<files>bot/db.py</files>
<action>
    In bot/db.py, update add_pending_approval to include analysis_breakdown in INSERT:
    
    Add analysis_breakdown to the columns list and values tuple.
    
    Also update get_pending_approvals to return this column.
  </action>
<verify>grep -n "analysis_breakdown" bot/db.py</verify>
  <done>Database operations include analysis_breakdown</done>
</task>

<task type="auto">
  <name>Task 4: Update ApprovalQueue.tsx to display analysis_breakdown fields</name>
<files>client/src/components/ApprovalQueue.tsx</files>
<action>
    In ApprovalQueue.tsx:
    
    1. Update AnalysisData interface to include analysis_breakdown fields:
       - current_price, rolling_avg, deviation (for momentum)
       - sentiment, headline (for news)
       - beat_rate, consensus (for earnings)
    
    2. In AnalysisBreakdown component, display these fields when present:
       - If analysis_breakdown.direction exists: show "Oversold/Overbought" indicator
       - If analysis_breakdown.sentiment exists: show sentiment badge
       - If analysis_breakdown.beat_rate exists: show weighted beat rate
    
    3. Show confidence score prominently as a badge or colored indicator.
  </action>
<verify>grep -n "analysis_breakdown\|confidence" client/src/components/ApprovalQueue.tsx | head -10</verify>
  <done>Dashboard displays full analysis breakdown with confidence score</done>
</task>

</tasks>

<verification>
- [ ] Schema updated, db push successful
- [ ] Python bot stores analysis_breakdown in pending_approvals
- [ ] Dashboard displays confidence score 0-100 for each signal
- [ ] Analysis breakdown shows agent-specific fields
</verification>

<success_criteria>
User can see confidence score and full analysis breakdown for each opportunity on dashboard.
</success_criteria>

<output>
After completion, create `.planning/phases/03-research-engine-polish/03-04-SUMMARY.md`
</output>