---
phase: 00-critical-fixes
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - bot/orchestrator.py
  - bot/agents/base_agent.py
autonomous: true

must_haves:
  truths:
    - "self.seen_tokens is thread-safe under concurrent access"
    - "agent_status.json is written atomically"
    - "Bot can handle concurrent approval responses without crashing"
  artifacts:
    - path: "bot/orchestrator.py"
      provides: "Thread-safe seen_tokens with threading.Lock"
  key_links:
    - from: "bot/orchestrator.py (_process_signals)"
      to: "bot/orchestrator.py (self._seen_lock)"
      via: "with self._seen_lock"
      pattern: "_seen_lock"
    - from: "bot/orchestrator.py (_approve_and_execute)"
      to: "bot/orchestrator.py (self._seen_lock)"
      via: "with self._seen_lock"
      pattern: "_seen_lock"
---

<objective>
Fix thread safety issues: seen_tokens race condition, agent_status.json atomic writes.
</objective>

<context>
@.planning/ROADMAP.md
@C:\Users\kittu\Downloads\GIT\Pollyedge\pollyedge\bot\orchestrator.py
@C:\Users\kittu\Downloads\GIT\Pollyedge\pollyedge\bot\agents\base_agent.py
</context>

<tasks>

<task type="auto">
  <name>Add threading.Lock to self.seen_tokens</name>
  <files>bot/orchestrator.py</files>
  <action>
    In `bot/orchestrator.py`:

    1. Add `import threading` at top of file (if not present)
    2. In `__init__`, after `self.seen_tokens = set()` add:
       ```python
       self._seen_lock = threading.Lock()
       ```
    3. In `_process_signals()`, wrap the seen_tokens check:
       ```python
       with self._seen_lock:
           if signal["token_id"] in self.seen_tokens:
               continue
       ```
    4. In `_approve_and_execute()`, wrap the add:
       ```python
       with self._seen_lock:
           self.seen_tokens.add(signal["token_id"])
       ```
  </action>
  <verify>python -c "from bot.orchestrator import Orchestrator; o = Orchestrator(); print('seen_lock:', hasattr(o, '_seen_lock'))"</verify>
  <done>seen_tokens is protected by threading.Lock in all access points</done>
</task>

<task type="auto">
  <name>Fix agent_status.json to write atomically</name>
  <files>bot/agents/base_agent.py</files>
  <action>
    In `bot/agents/base_agent.py`, check the `_write_status()` method. It currently writes `agent_status.json` but may not be atomic. Ensure:
    1. Uses a temporary file + rename for atomic writes
    2. Uses a thread lock (`_status_lock`) to prevent concurrent writes
    3. Uses `json.dumps` with proper indentation
    4. File is written to temp path, then renamed

    If `_write_status()` doesn't exist, create it in `BaseAgent` with proper thread safety.
  </action>
  <verify>python -c "from bot.agents.base_agent import BaseAgent; import threading; print('_status_lock' in dir(BaseAgent))"</verify>
  <done>agent_status.json writes are atomic and thread-safe</done>
</task>

</tasks>

<verification>
1. Start bot and trigger multiple signals concurrently
2. Check agent_status.json is valid JSON (not truncated/corrupted)
3. Verify seen_tokens doesn't cause race condition under load
</verification>

<success_criteria>
- No race conditions under concurrent signal processing
- agent_status.json is always valid JSON after writes
- Multiple agents can write status simultaneously without corruption
</success_criteria>

<output>
After completion, create `.planning/phases/00-critical-fixes/00-03-SUMMARY.md`
</output>
