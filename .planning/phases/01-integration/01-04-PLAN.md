---
phase: 01-integration
plan: 04
type: execute
wave: 2
depends_on: [1]
files_modified:
  - bot/approval_gate.py
autonomous: true

must_haves:
  truths:
    - "Python writes pending approvals to SQLite"
    - "TypeScript reads from same SQLite database"
    - "Dashboard shows live approval queue from SQLite"
  artifacts:
    - path: "bot/approval_gate.py"
      provides: "Pending approvals written to SQLite"
  key_links:
    - from: "bot/approval_gate.py"
      to: "bot/db.py"
      via: "from bot.db import add_pending_approval, resolve_pending_approval"
      pattern: "add_pending_approval"
    - from: "bot/approval_gate.py"
      to: "pending_approvals.json"
      via: "json.dump (to be removed)"
      pattern: "APPROVALS_FILE"
---

<objective>
Migrate pending_approvals.json to SQLite. Python writes, TypeScript reads from the same database.
</objective>

<context>
@.planning/ROADMAP.md
@C:\Users\kittu\Downloads\GIT\Pollyedge\pollyedge\bot\approval_gate.py
@C:\Users\kittu\Downloads\GIT\Pollyedge\pollyedge\bot\db.py
</context>

<tasks>

<task type="auto">
  <name>Add pending approval functions to bot/db.py</name>
  <files>bot/db.py</files>
  <action>
    Add to `bot/db.py`:

    1. `add_pending_approval(entry: dict)` — insert into pending_approvals table
       - Fields: id, label, side, size, edge, source, score, market_prob, model_prob, timestamp, status
    2. `get_pending_approvals()` — returns list, filtered by status="pending", ordered by timestamp desc
    3. `resolve_pending_approval(id: str, status: str)` — update status field
    4. Keep last 20 entries (delete older ones on add)

    Also: add `delete_pending_approval(id: str)` helper.
  </action>
  <verify>py -c "from bot.db import add_pending_approval, resolve_pending_approval; print('pending approval functions OK')"</verify>
  <done>bot/db.py has pending approval functions</done>
</task>

<task type="auto">
  <name>Migrate approval_gate.py to use SQLite</name>
  <files>bot/approval_gate.py</files>
  <action>
    Update `bot/approval_gate.py`:

    1. Remove: `import json`, `APPROVALS_FILE`, `_approvals_lock`
    2. Remove: `_read_pending()`, `_write_pending()`, `_resolve_pending()` (replace with db calls)
    3. Add: `from bot.db import add_pending_approval, resolve_pending_approval`
    4. Replace `_write_pending()` calls with `add_pending_approval(entry)`
    5. Replace `_resolve_pending()` calls with `resolve_pending_approval(msg_id_key, status)`
    6. Replace `_read_pending()` calls with `get_pending_approvals()`
    7. Remove the pending dict from memory tracking — use database as source of truth
    8. Keep the existing Telegram message sending logic intact
    9. Remove `pending_approvals.json` file writes entirely

    The approval flow stays the same (Telegram inline buttons), but data goes to SQLite.
  </action>
  <verify>py -c "from bot.approval_gate import ApprovalGate; print('approval_gate imports OK')"</verify>
  <done>approval_gate.py writes pending approvals to SQLite</done>
</task>

</tasks>

<verification>
1. Bot generates a signal requiring approval
2. Check `data/pollyedge.db` — pending_approvals table has the entry
3. Dashboard reads from SQLite — ApprovalQueue shows the pending trade
4. Approve via Telegram — status updated to "approved" in SQLite
5. Dashboard reflects the approval
6. pending_approvals.json file is no longer created
</verification>

<success_criteria>
- approval_gate.py uses SQLite (no JSON file writes)
- Dashboard shows live approval queue from SQLite
- Approval statuses update correctly after Telegram interaction
- pending_approvals.json is no longer written
</success_criteria>

<output>
After completion, create `.planning/phases/01-integration/01-04-SUMMARY.md`
</output>
