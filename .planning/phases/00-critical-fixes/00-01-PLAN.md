---
phase: 00-critical-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - bot/agents/momentum_agent.py
  - bot/orchestrator.py
autonomous: true

must_haves:
  truths:
    - "Bot starts without NameError for MomentumAgent"
    - "All 5 agents load successfully"
    - "MomentumAgent generates signals independently"
  artifacts:
    - path: "bot/agents/momentum_agent.py"
      provides: "5th agent for price mean-reversion"
      min_lines: 80
  key_links:
    - from: "bot/orchestrator.py"
      to: "bot/agents/momentum_agent.py"
      via: "import + agent instantiation"
      pattern: "MomentumAgent"
---

<objective>
Create the missing MomentumAgent file and fix orchestrator imports so all 5 agents load correctly.
</objective>

<context>
@.planning/ROADMAP.md
@C:\Users\kittu\Downloads\GIT\Pollyedge\pollyedge\bot\orchestrator.py
@C:\Users\kittu\Downloads\GIT\Pollyedge\pollyedge\bot\agents\base_agent.py
@C:\Users\kittu\Downloads\GIT\Pollyedge\pollyedge\bot\agents\crypto_agent.py
</context>

<tasks>

<task type="auto">
  <name>Create MomentumAgent with price mean-reversion logic</name>
  <files>bot/agents/momentum_agent.py</files>
  <action>
    Create `bot/agents/momentum_agent.py` following the same pattern as other agents:

    1. Inherit from `BaseAgent`
    2. Poll Polymarket/CLOB API every 5 minutes for top markets
    3. Detect mean-reversion opportunities: when price deviates significantly from historical average (use 24h or 7d data)
    4. Score signals based on deviation magnitude and volume
    5. Enqueue signals via `self.signal_q.put(signal)`

    The agent should:
    - Use `py_clob_client` or `requests` to fetch Polymarket market data
    - Calculate deviation from 24h average price
    - Only signal when deviation exceeds threshold (e.g., 5% from mean)
    - Include momentum/mean-reversion label in signal

    Reference: `bot/agents/arb_agent.py` for CLOB API calls, `bot/agents/crypto_agent.py` for signal format
  </action>
  <verify>python -c "from bot.agents.momentum_agent import MomentumAgent; print('MomentumAgent imports OK')"</verify>
  <done>MomentumAgent class exists and can be instantiated without errors</done>
</task>

<task type="auto">
  <name>Fix orchestrator to import MomentumAgent</name>
  <files>bot/orchestrator.py</files>
  <action>
    In `bot/orchestrator.py`, add the import for MomentumAgent:

    ```python
    from bot.agents.momentum_agent import MomentumAgent
    ```

    Make sure it appears in the agents list where referenced (currently referenced by name but not imported).
  </action>
  <verify>python -c "from bot.orchestrator import Orchestrator; print('Orchestrator imports OK')"</verify>
  <done>Orchestrator imports MomentumAgent without NameError</done>
</task>

</tasks>

<verification>
1. Run `python -c "from bot.orchestrator import Orchestrator; print('Import OK')"` — should succeed
2. Verify `bot/agents/momentum_agent.py` has `class MomentumAgent(BaseAgent)` 
3. Verify orchestrator has `from bot.agents.momentum_agent import MomentumAgent`
</verification>

<success_criteria>
- `python -m bot.pollyedge_bot` starts without NameError
- All 5 agents appear in agent_status.json after startup
- MomentumAgent runs its scan loop independently
</success_criteria>

<output>
After completion, create `.planning/phases/00-critical-fixes/00-01-SUMMARY.md`
</output>
